# charlietrenorden.com
# Everything here is meant to be crawlable. The only thing worth keeping out is the
# private stats dashboard, which is key-gated anyway - this is belt and braces, and a
# reminder that a robots rule is NOT a security control.
User-agent: *
Allow: /
Disallow: /stats/

# Projects served at a path under this domain ship their OWN sitemap, and a
# robots.txt is only honoured at the domain ROOT - so /chronoscape/robots.txt is
# ignored by crawlers and its sitemap would never be found. List it here instead.
Sitemap: https://charlietrenorden.com/sitemap.xml
Sitemap: https://charlietrenorden.com/chronoscape/sitemap.xml